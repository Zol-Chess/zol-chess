/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBooleanDecoder,
  getBooleanEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU8Decoder,
  getU8Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { findPuzzleHistoryPda, findSubmitPuzzlePlayerInfoPda } from "../pdas";
import { ZOL_CHESS_PROGRAM_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const SUBMIT_PUZZLE_DISCRIMINATOR = new Uint8Array([
  143, 153, 182, 92, 120, 71, 16, 84,
]);

export function getSubmitPuzzleDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    SUBMIT_PUZZLE_DISCRIMINATOR,
  );
}

export type SubmitPuzzleInstruction<
  TProgram extends string = typeof ZOL_CHESS_PROGRAM_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountPuzzleHistory extends string | AccountMeta<string> = string,
  TAccountPlayerInfo extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountPuzzleHistory extends string
        ? WritableAccount<TAccountPuzzleHistory>
        : TAccountPuzzleHistory,
      TAccountPlayerInfo extends string
        ? WritableAccount<TAccountPlayerInfo>
        : TAccountPlayerInfo,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type SubmitPuzzleInstructionData = {
  discriminator: ReadonlyUint8Array;
  puzzleId: string;
  puzzleRating: number;
  timeTaken: number;
  solved: boolean;
  attempts: number;
  solutionSignature: ReadonlyUint8Array;
};

export type SubmitPuzzleInstructionDataArgs = {
  puzzleId: string;
  puzzleRating: number;
  timeTaken: number;
  solved: boolean;
  attempts: number;
  solutionSignature: ReadonlyUint8Array;
};

export function getSubmitPuzzleInstructionDataEncoder(): Encoder<SubmitPuzzleInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["puzzleId", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ["puzzleRating", getU32Encoder()],
      ["timeTaken", getU32Encoder()],
      ["solved", getBooleanEncoder()],
      ["attempts", getU8Encoder()],
      ["solutionSignature", fixEncoderSize(getBytesEncoder(), 64)],
    ]),
    (value) => ({ ...value, discriminator: SUBMIT_PUZZLE_DISCRIMINATOR }),
  );
}

export function getSubmitPuzzleInstructionDataDecoder(): Decoder<SubmitPuzzleInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["puzzleId", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ["puzzleRating", getU32Decoder()],
    ["timeTaken", getU32Decoder()],
    ["solved", getBooleanDecoder()],
    ["attempts", getU8Decoder()],
    ["solutionSignature", fixDecoderSize(getBytesDecoder(), 64)],
  ]);
}

export function getSubmitPuzzleInstructionDataCodec(): Codec<
  SubmitPuzzleInstructionDataArgs,
  SubmitPuzzleInstructionData
> {
  return combineCodec(
    getSubmitPuzzleInstructionDataEncoder(),
    getSubmitPuzzleInstructionDataDecoder(),
  );
}

export type SubmitPuzzleAsyncInput<
  TAccountAuthority extends string = string,
  TAccountPuzzleHistory extends string = string,
  TAccountPlayerInfo extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  puzzleHistory?: Address<TAccountPuzzleHistory>;
  playerInfo?: Address<TAccountPlayerInfo>;
  systemProgram?: Address<TAccountSystemProgram>;
  puzzleId: SubmitPuzzleInstructionDataArgs["puzzleId"];
  puzzleRating: SubmitPuzzleInstructionDataArgs["puzzleRating"];
  timeTaken: SubmitPuzzleInstructionDataArgs["timeTaken"];
  solved: SubmitPuzzleInstructionDataArgs["solved"];
  attempts: SubmitPuzzleInstructionDataArgs["attempts"];
  solutionSignature: SubmitPuzzleInstructionDataArgs["solutionSignature"];
};

export async function getSubmitPuzzleInstructionAsync<
  TAccountAuthority extends string,
  TAccountPuzzleHistory extends string,
  TAccountPlayerInfo extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof ZOL_CHESS_PROGRAM_PROGRAM_ADDRESS,
>(
  input: SubmitPuzzleAsyncInput<
    TAccountAuthority,
    TAccountPuzzleHistory,
    TAccountPlayerInfo,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  SubmitPuzzleInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountPuzzleHistory,
    TAccountPlayerInfo,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ZOL_CHESS_PROGRAM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    puzzleHistory: { value: input.puzzleHistory ?? null, isWritable: true },
    playerInfo: { value: input.playerInfo ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.puzzleHistory.value) {
    accounts.puzzleHistory.value = await findPuzzleHistoryPda({
      authority: expectAddress(accounts.authority.value),
    });
  }
  if (!accounts.playerInfo.value) {
    accounts.playerInfo.value = await findSubmitPuzzlePlayerInfoPda({
      authority: expectAddress(accounts.authority.value),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.puzzleHistory),
      getAccountMeta(accounts.playerInfo),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getSubmitPuzzleInstructionDataEncoder().encode(
      args as SubmitPuzzleInstructionDataArgs,
    ),
    programAddress,
  } as SubmitPuzzleInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountPuzzleHistory,
    TAccountPlayerInfo,
    TAccountSystemProgram
  >);
}

export type SubmitPuzzleInput<
  TAccountAuthority extends string = string,
  TAccountPuzzleHistory extends string = string,
  TAccountPlayerInfo extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  puzzleHistory: Address<TAccountPuzzleHistory>;
  playerInfo: Address<TAccountPlayerInfo>;
  systemProgram?: Address<TAccountSystemProgram>;
  puzzleId: SubmitPuzzleInstructionDataArgs["puzzleId"];
  puzzleRating: SubmitPuzzleInstructionDataArgs["puzzleRating"];
  timeTaken: SubmitPuzzleInstructionDataArgs["timeTaken"];
  solved: SubmitPuzzleInstructionDataArgs["solved"];
  attempts: SubmitPuzzleInstructionDataArgs["attempts"];
  solutionSignature: SubmitPuzzleInstructionDataArgs["solutionSignature"];
};

export function getSubmitPuzzleInstruction<
  TAccountAuthority extends string,
  TAccountPuzzleHistory extends string,
  TAccountPlayerInfo extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof ZOL_CHESS_PROGRAM_PROGRAM_ADDRESS,
>(
  input: SubmitPuzzleInput<
    TAccountAuthority,
    TAccountPuzzleHistory,
    TAccountPlayerInfo,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): SubmitPuzzleInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountPuzzleHistory,
  TAccountPlayerInfo,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ZOL_CHESS_PROGRAM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    puzzleHistory: { value: input.puzzleHistory ?? null, isWritable: true },
    playerInfo: { value: input.playerInfo ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.puzzleHistory),
      getAccountMeta(accounts.playerInfo),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getSubmitPuzzleInstructionDataEncoder().encode(
      args as SubmitPuzzleInstructionDataArgs,
    ),
    programAddress,
  } as SubmitPuzzleInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountPuzzleHistory,
    TAccountPlayerInfo,
    TAccountSystemProgram
  >);
}

export type ParsedSubmitPuzzleInstruction<
  TProgram extends string = typeof ZOL_CHESS_PROGRAM_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    puzzleHistory: TAccountMetas[1];
    playerInfo: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: SubmitPuzzleInstructionData;
};

export function parseSubmitPuzzleInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedSubmitPuzzleInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      authority: getNextAccount(),
      puzzleHistory: getNextAccount(),
      playerInfo: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getSubmitPuzzleInstructionDataDecoder().decode(instruction.data),
  };
}
